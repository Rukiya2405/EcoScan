name: ecoscan
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  image_picker: ^1.2.3

  firebase_core: ^4.14.0
  firebase_auth: ^6.6.1
  cloud_firestore: ^6.9.0
  firebase_messaging: ^16.6.0
  flutter_local_notifications: ^18.0.0
  firebase_storage: 13.5.0        # ADD THIS

  geolocator: ^14.0.3
  google_maps_flutter: ^2.18.0
  firebase_ai: ^4.0.0
  firebase_app_check: ^0.4.7
  image: ^4.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true